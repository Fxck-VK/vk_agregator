# Current Handoff

Status: none

There is no active handoff at the moment.

Use this file only when a current cross-agent handoff is explicitly in progress.
After the handoff or merge is complete, move its contents to
`docs/archive/handoffs/` and reset this file back to `Status: none`.

Do not read archived handoff files by default. Use `docs/INDEX.md` for
documentation routing.
