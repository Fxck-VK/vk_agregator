# Отчёт о проверке модели

Статус: черновик. Это шаблон, а не подтверждение выполненных проверок.

- Public ID / провайдер / provider model ID:
- Версия / операция / endpoint:
- Registry fingerprint / contract digest:
- Проверявший / дата / ревизия кода:
- Источники документации и даты фактического чтения:
- Разрешённый DEV-контур, живые сценарии и предел расходов:
- Фактические расходы / непроверенные возможности:

Заменить `reply` на ID операции и повторить разделы для каждой операции.
Дополнить разделами каждого включённого формата вложений. В каждом разделе
записать ожидаемое и фактическое поведение, команду/метод проверки, охват,
дату и ограничения. До выполнения использовать `not_run`.

## reply/adapter

not_run — проверить передачу параметров и вложений в адаптере.

## reply/negative

not_run — недопустимые файлы и параметры отклонены до платного submit.

## reply/boundaries

not_run — проверить минимумы, максимумы, превышение и комбинации.

## reply/pricing

not_run — цены покрывают включённые варианты; проверить расчёт и списание.

## reply/job-lifecycle

not_run — права на файлы, ошибки, повторы, timeout, резерв/capture/release.

## reply/catalog

not_run — каталог, параметры интерфейса и server validation согласованы.

## reply/live-output

not_run — реальный результат получен и проверен по содержимому/метаданным.

## reply/live-input/documents/.pdf

not_run — пример сценария; оставить только для действительно включённого PDF.
Задать вопрос по содержимому синтетического документа, которого нет в промпте.

## Решение

Не допущено. Указать, что можно включить после проверки и что остаётся выключено.
