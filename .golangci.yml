version: "2"

run:
  timeout: 5m
  relative-path-mode: gomod
  tests: true

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - unused

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
